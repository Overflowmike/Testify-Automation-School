// Create a length converter function
// Conversion of meter to kilometer
// Conversion rate is 1000 meter is to 1km


function converter(kilometer) {
    const meter = kilometer * 1000;
    return kilometer;
}
const kilometerValue = converter(10);
console.log(kilometerValue)